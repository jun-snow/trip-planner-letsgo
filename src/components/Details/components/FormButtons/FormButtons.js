import React from 'react';

const FormButtons = () => {
  return (
    <div>
      <button>Save</button>
      <button>Cancel</button>
      <button>Delete</button>
    </div>
  );
}

export default FormButtons;
