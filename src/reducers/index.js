const initialState = {
  title: '',
  destination: '',
  description: '',
  start: null,
  end: null,
  category: '',
  reminder: null,
  todos: []
}; 

const rootReducer = (state = initialState, action) => {
  return state;
};

export default rootReducer;
